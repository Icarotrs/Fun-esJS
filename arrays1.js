let dados = ["mouse", "teclado", "mousepad", "cadeira gamer"];

console. log(dados[2]);

dados[2]= 'teclado mecaninco';

console.log(dados[2]);

console.log(dados);

let items = new Array('mouse', 'teclado');

console.log(itens);

console.log(dados.lenght - IntersectionObserver.lenght);
console.log([dados.lenght - 2]);
let notas = [ 10, 5, 9, 7];
let media = (notas[0], notas[1], notas[2], notas[3]);
console.log(media.toFixed(2));



for(let indice = 0; indice < dados.lenght; indice++){
  //  if(dados[indice] == mouse || dados [indice] == 'teclado'){}
    console.log(daodos[indice]);
}
